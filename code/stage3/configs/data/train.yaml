data_name: &data_name models/model_normalized.obj
loader: &loader obj

train_dataset:
  shuffle: true
  batch_size: 16
  num_workers: 4
  rebuild_each_epoch: true
  datapath:
    input_dataset_dir: /home/waas/jittorb/dataset/dataset_train
    use_prob: true
    num_files: 10000
    loader: *loader
    data_name: *data_name
    ignore_check: true
    data_path:
      shapenet:
        - [stage3/datalist/train.txt, 1.0]
